// .gitkeep is used to keep the empty folders in the git repository. It is a placeholder file that allows git to track the folder even if it is empty. This is useful for maintaining the structure of the project and ensuring that all necessary folders are included in the repository.

git doesnt track empty folders so we created gitkeep file to keep the folders in track